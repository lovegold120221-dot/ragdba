Can you be intelligent enough to help on how you create the components of the front end? For example, these languages from the most right header.

If you can make a dropdown, not a dropdown, I mean, you can make it a dropdown, but it'll be a list of horizontal. It will add so much space. As much as possible, we don't want so much noise in the UI. So, we just want a very intuitive one. Even though there's a lot of writing in the bottom area of this. We tend not to want this one.

And then just add an attach files icon also on the most left of the input field. Probably at least that it's separated, the files versus images, versus audio because it can record an audio, for prompting.

So I want you to do to handle this one properly. And that's very important that you need to have this attachment and make the saving of the data persistent on the.

Probably a good thing you could use is PostgreSQL and Prisma DB. Below this, on the sign-in with Google button for the web app view, create an upload functionality.
The upload functionality allows users to upload files.
If the user uploads files, the system can use it as a rag data, or the AI can answer what is on that type of documents when the user uploads it. I think we just need to use light mode on this whole app. We don't need to use the black background anymore. Just use light mode, but make it high contrast. Can you make the sidebar collapsible?
Can we make the search native?
If users only need the URL, we can provide it.
As much as possible, we should take the data from the URL and feed it to the users.
We don't need to send them to the URLs because we have it for a reason, to avoid them going there.
They can ask us directly.
That's how it should be done.
